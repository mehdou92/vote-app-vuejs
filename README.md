# VOTE-APP in golang

### Start the project 

`make up` (with sudo if u need)

### Stop the project 

`make down`

### Seed the database with fixture

go the file `fixtures/fixtures_test.go` and run the script to seed the db